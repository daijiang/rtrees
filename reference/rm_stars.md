# Remove trailing \*

Remove trailing \*

## Usage

``` r
rm_stars(tree)
```

## Arguments

- tree:

  A phylogeny generated by `get_tree(..., show_grafted = TRUE)` with
  trailing stars in tip labels.

## Value

A phylogeny after removing trailing stars.
